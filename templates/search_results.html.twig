{% extends 'base.html.twig' %}

{% block title %}Résultats de recherche{% endblock %}

{% block body %}

    <div class="container mt-5">
        <h1 class="mb-4">Résultats pour "{{ search }}"</h1>

        <!-- Catégories -->
        <div class="mb-4">
            <h2 class="text-primary">Catégorie(s)</h2>
            {% if categories|length > 0 %}
                <div class="p-3 mb-3" style="background-color: #d4edda; color: #155724; border-radius: 5px;">
                    🎉 {{ categories|length }} catégorie(s) trouvée(s).
                </div>
                <ul class="list-group">
                    {% for category in categories %}
                        <li class="list-group-item">{{ category.name }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="p-3 mb-3" style="background-color: #f8d7da; color: #721c24; border-radius: 5px;">
                    Aucun catégorie trouvée.
                </div>
            {% endif %}
        </div>

        <!-- Événements -->
        <div class="mb-4">
            <h2 class="text-primary">Événement(s)</h2>
            {% if events|length > 0 %}
                <div class="p-3 mb-3" style="background-color: #d4edda; color: #155724; border-radius: 5px;">
                    🎉 {{ events|length }} événement(s) trouvé(s).
                </div>
                <ul class="list-group">
                    {% for event in events %}
                        <li class="list-group-item">
                            <strong>{{ event.title }}</strong> -
                            <span class="text-muted">{{ event.startTime|date('d/m/Y') }}</span>
                        </li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="p-3 mb-3" style="background-color: #f8d7da; color: #721c24; border-radius: 5px;">
                    Aucun événement trouvé.
                </div>
            {% endif %}
        </div>

        <!-- Animateurs -->
        <div class="mb-4">
            <h2 class="text-primary">Animateur(s)</h2>
            {% if animators|length > 0 %}
                <div class="p-3 mb-3" style="background-color: #d4edda; color: #155724; border-radius: 5px;">
                    🎉 {{ animators|length }} animateur(s) trouvé(s).
                </div>
                <ul class="list-group">
                    {% for animator in animators %}
                        <li class="list-group-item">{{ animator.firstname }} {{ animator.lastname }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="p-3 mb-3" style="background-color: #f8d7da; color: #721c24; border-radius: 5px;">
                    Aucun animateur trouvé.
                </div>
            {% endif %}
        </div>

        <!-- Salles -->
        <div class="mb-4">
            <h2 class="text-primary">Salle(s)</h2>
            {% if rooms|length > 0 %}
                <div class="p-3 mb-3" style="background-color: #d4edda; color: #155724; border-radius: 5px;">
                    🎉 {{ rooms|length }} salle(s) trouvée(s).
                </div>
                <ul class="list-group">
                    {% for room in rooms %}
                        <li class="list-group-item">{{ room.name }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="p-3 mb-3" style="background-color: #f8d7da; color: #721c24; border-radius: 5px;">
                    Aucune salle trouvée.
                </div>
            {% endif %}
        </div>

        <!-- Établissements -->
        <div class="mb-4">
            <h2 class="text-primary">Établissement(s)</h2>
            {% if establishments|length > 0 %}
                <div class="p-3 mb-3" style="background-color: #d4edda; color: #155724; border-radius: 5px;">
                    🎉 {{ establishments|length }} établissement(s) trouvé(s).
                </div>
                <ul class="list-group">
                    {% for establishment in establishments %}
                        <li class="list-group-item">{{ establishment.name }}</li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="p-3 mb-3" style="background-color: #f8d7da; color: #721c24; border-radius: 5px;">
                    Aucun établissement trouvé.
                </div>
            {% endif %}
        </div>
    </div>

{% endblock %}
